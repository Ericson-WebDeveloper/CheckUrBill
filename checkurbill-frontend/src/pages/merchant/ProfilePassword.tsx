import React from 'react'

type ProfilePasswordProps = {}

const ProfilePassword = (props: ProfilePasswordProps) => {
  return (
    <div>Merchant ProfilePassword</div>
  )
}

export default ProfilePassword